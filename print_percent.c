#include "main.h"
/**
 * print_percent - a function that prints '%'
 * @args: a pointer to the character
 * Return: total of characters
*/
int print_percent(va_list args) {

	(void)args;
	_putchar('%');
	return (1);
}